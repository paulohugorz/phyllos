[build]
builder = "nixpacks"

[deploy]
startCommand = "cd _legado && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
